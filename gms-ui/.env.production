# ============================================================
#  生产构建配置（yarn build，产物打包进 jar）
# ============================================================
# 这里故意留空：打包进 jar 后前端和后端是同一个端口，
# 走同源相对路径，不需要跨域，也不需要写死 IP。
#
# 只有想把前端单独部署到别的域名时，才在这里写上后端地址，例如：
#     VITE_API_BASE_URL=https://api.example.com
